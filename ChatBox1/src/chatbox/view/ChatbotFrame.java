package chatbox.view;

public class ChatbotFrame
{
	private void setupListeners()

	{
	submit.addActionListener(new ActionListener()) 
	{
		
		public void actionPerformed(new Actionlistener())
		{
			
			{
				clickcount++;
				String currentInput = user + InputField.getText();
				chatArea.append("\n" + currentInput);
				chatArea.append("\n" + appBot.getRandomTopic());
				
				if (clickCount % 7 = 0 )
				{
					chatArea.append(baseController.processConversation());
					clickcount--;
				}
			}
		}

	}
}